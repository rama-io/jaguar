# Consumer ProGuard rules for bohio.
# Add -keep rules here for anything reflectively accessed
# (e.g. if StorageManager.storageVolumes reflection fallback
# from the SAF work needs to survive minification in consuming apps).
